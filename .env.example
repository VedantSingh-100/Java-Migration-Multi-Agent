# Amazon Bedrock API Key (required)
# This is a long-term Bedrock API key (ABSK format) used as bearer token
AWS_BEARER_TOKEN_BEDROCK=your-bedrock-api-key-here

# AWS Region for Bedrock (defaults to us-east-1)
AWS_REGION=us-east-1

# Optional: For legacy Anthropic API (not used in refactored version)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
